vault:
  image:
    tag: 0.9.3
  ingress:
    enabled: false
    # Used to create Ingress record (should used with service.type: ClusterIP).
    # hosts:
    #  - chart-example.local
    # annotations:
    #   kubernetes.io/ingress.class: nginx
    #   kubernetes.io/tls-acme: "true"
    # tls:
    #   Secrets must be manually created in the namespace.
    #   - secretName: chart-example-tls
    #     hosts:
    #       - chart-example.local
  vault: 
    dev: false
    config:
      storage:
        consul:
          address: http://issa-consul:8500
          path: vault
  
          
consul:
  ImageTag: 1.0.6
  Replicas: 1
  ui:
    enabled: false
  uiService:
    enabled	: false