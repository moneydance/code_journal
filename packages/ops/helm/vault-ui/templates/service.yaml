apiVersion: v1
kind: Service
metadata:
  name: {{ template "vault-ui.fullname" . }}
  labels:
    app: {{ template "vault-ui.name" . }}
    chart: {{ template "vault-ui.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "vault-ui.name" . }}
    release: {{ .Release.Name }}
